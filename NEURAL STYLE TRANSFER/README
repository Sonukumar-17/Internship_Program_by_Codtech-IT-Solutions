# Neural Style Transfer

## 📌 Overview
This project applies artistic styles to photographs using a **Neural Style Transfer** model built with PyTorch and VGG19.

## 🚀 How to Run
1. Place your content image (`content.jpg`) and style image (`style.jpg`) in the project folder.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
